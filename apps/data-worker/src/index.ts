// Placeholder entry point for the forthcoming TypeScript data worker.
// Replace with the actual worker bootstrap once queue consumers are ported.

import './env';

console.log('data-worker bootstrap pending');
