#!/bin/sh

set -e

# Ensure the hook runs from the repository root
cd "$(dirname "$0")/.."

echo "Linting..."
bun run lint

echo "Type checking..."
bunx tsc

git add .

echo "Pre-commit checks passed!"
